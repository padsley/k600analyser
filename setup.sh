#!/bin/bash


#This code will, ideally, become the setup script for running things, where you can set the detector types, the ADC channels etc. etc. etc. It currently does nothing because I haven't managed to make it yet.

# g++ setup.cpp -o setup
#./setup;
#Now remake the whole code with the new parameters 
make clean && make